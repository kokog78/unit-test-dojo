export class MarsRoverNavigation {

	/**
	 * Navigációs metódus a marsjáróhoz.
	 * @param zoneSize a zóna mérete, két egész szám egymástól egy szóközzel elválasztva
	 * @param startingFrom a kiindulási pozíció, két egész szám és egy nagybetű, egymástól szóközökkel elválasztva
	 * @param commands a végrehajtandó parancsok, több nagybetű egymás után
	 * @return a végső pozíció, két egész szám és egy nagybetű, egymástól szóközökkel elválasztva
	 */
	navigate(zoneSize: string, startingFrom: string, commands: string) {

	}

}
